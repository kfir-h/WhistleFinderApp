name: Build APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '17'
    
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew || true
    
    - name: Create gradlew if missing
      run: |
        if [ ! -f gradlew ]; then
          cat > gradlew << 'EOF'
        #!/bin/sh
        exec gradle "$@"
        EOF
          chmod +x gradlew
        fi
    
    - name: Create gradle wrapper
      run: |
        mkdir -p gradle/wrapper
        cat > gradle/wrapper/gradle-wrapper.properties << 'EOF'
        distributionUrl=https\://services.gradle.org/distributions/gradle-8.2-bin.zip
        EOF
    
    - name: Build with Gradle
      run: ./gradlew assembleDebug --stacktrace
    
    - name: Upload APK
      uses: actions/upload-artifact@v3
      with:
        name: app-debug
        path: app/build/outputs/apk/debug/app-debug.apk
```

5. Click **"Commit changes..."** button (green button top right)
6. Click **"Commit changes"** again in the popup

---

### Step 3: Go Back to Actions

1. Click the **"Actions"** tab again
2. Now you should see **"Build APK"** appear on the LEFT sidebar
3. Click on **"Build APK"**
4. Click the blue **"Run workflow"** button on the right
5. Click the green **"Run workflow"** button in the dropdown
6. **Wait 5-10 minutes** for the build to complete

---

### Step 4: Watch the Build

You'll see:
- 🟡 **Yellow circle** = Building (be patient!)
- ✅ **Green checkmark** = Success! Download your APK!
- ❌ **Red X** = Failed (tell me the error)

---

## 📝 QUICK SUMMARY
```
1. Code tab → Add file → Create new file
2. Filename: .github/workflows/build.yml
3. Paste the code above
4. Commit changes
5. Actions tab → Build APK → Run workflow
6. Wait → Download from Artifacts
